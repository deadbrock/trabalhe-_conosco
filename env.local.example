# Copie para .env.local na raiz do projeto (mesmo nível de package.json do Next.js)
# O login do painel chama a API Nest/Express — ela precisa estar rodando (porta 3333).

NEXT_PUBLIC_API_BASE=http://localhost:3333
